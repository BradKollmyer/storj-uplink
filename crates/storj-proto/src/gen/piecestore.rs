// This file is @generated by prost-build.
/// Expected order of messages from uplink:
///
///    OrderLimit ->
///    repeated
///       Order ->
///       Chunk ->
///    PieceHash signed by uplink ->
///       <- PieceHash signed by storage node
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceUploadRequest {
    /// first message to show that we are allowed to upload
    #[prost(message, optional, tag = "1")]
    pub limit: ::core::option::Option<super::orders::OrderLimit>,
    /// first message must have it if (!) not the default sha256 is used, as it
    /// should be initialized by the storagenode before upload.
    /// should match with the algorithm in the done field of the last message
    #[prost(enumeration = "super::orders::PieceHashAlgorithm", tag = "5")]
    pub hash_algorithm: i32,
    /// order for uploading
    #[prost(message, optional, tag = "2")]
    pub order: ::core::option::Option<super::orders::Order>,
    #[prost(message, optional, tag = "3")]
    pub chunk: ::core::option::Option<piece_upload_request::Chunk>,
    /// final message
    #[prost(message, optional, tag = "4")]
    pub done: ::core::option::Option<super::orders::PieceHash>,
}
/// Nested message and enum types in `PieceUploadRequest`.
pub mod piece_upload_request {
    /// data message
    #[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
    pub struct Chunk {
        #[prost(int64, tag = "1")]
        pub offset: i64,
        #[prost(bytes = "vec", tag = "2")]
        pub data: ::prost::alloc::vec::Vec<u8>,
    }
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceUploadResponse {
    #[prost(message, optional, tag = "1")]
    pub done: ::core::option::Option<super::orders::PieceHash>,
    /// this is for validating the PieceHash signature if the cert chain is
    /// unable to be pulled off the connection (Noise instead of TLS).
    #[prost(bytes = "vec", tag = "2")]
    pub node_certchain: ::prost::alloc::vec::Vec<u8>,
}
/// Expected order of messages from uplink:
///
///    {OrderLimit, Chunk} ->
///    go repeated
///       Order -> (async)
///    go repeated
///       <- PieceDownloadResponse.Chunk
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceDownloadRequest {
    /// first message to show that we are allowed to upload
    #[prost(message, optional, tag = "1")]
    pub limit: ::core::option::Option<super::orders::OrderLimit>,
    /// order for downloading
    #[prost(message, optional, tag = "2")]
    pub order: ::core::option::Option<super::orders::Order>,
    /// request for the chunk
    #[prost(message, optional, tag = "3")]
    pub chunk: ::core::option::Option<piece_download_request::Chunk>,
    /// maximum_chunk_size is an advisory request from the uplink to
    /// the storage node on how big the chunks should be. smaller
    /// maximum sizes may reduce time to first byte and peak memory usage.
    #[prost(int32, tag = "4")]
    pub maximum_chunk_size: i32,
}
/// Nested message and enum types in `PieceDownloadRequest`.
pub mod piece_download_request {
    /// Chunk that we wish to download
    #[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
    pub struct Chunk {
        #[prost(int64, tag = "1")]
        pub offset: i64,
        #[prost(int64, tag = "2")]
        pub chunk_size: i64,
    }
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceDownloadResponse {
    #[prost(message, optional, tag = "1")]
    pub chunk: ::core::option::Option<piece_download_response::Chunk>,
    #[prost(message, optional, tag = "2")]
    pub hash: ::core::option::Option<super::orders::PieceHash>,
    #[prost(message, optional, tag = "3")]
    pub limit: ::core::option::Option<super::orders::OrderLimit>,
    #[prost(bool, tag = "4")]
    pub restored_from_trash: bool,
}
/// Nested message and enum types in `PieceDownloadResponse`.
pub mod piece_download_response {
    /// Chunk response for download request
    #[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
    pub struct Chunk {
        #[prost(int64, tag = "1")]
        pub offset: i64,
        #[prost(bytes = "vec", tag = "2")]
        pub data: ::prost::alloc::vec::Vec<u8>,
    }
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceDeleteRequest {
    #[prost(message, optional, tag = "1")]
    pub limit: ::core::option::Option<super::orders::OrderLimit>,
}
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceDeleteResponse {}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct DeletePiecesRequest {
    #[prost(bytes = "vec", repeated, tag = "1")]
    pub piece_ids: ::prost::alloc::vec::Vec<::prost::alloc::vec::Vec<u8>>,
}
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct DeletePiecesResponse {
    #[prost(int64, tag = "1")]
    pub unhandled_count: i64,
}
/// RetainRequest contains bloom filter. It's either sent together (time + filter) or in multiple chunks, where
/// first message contains time + first part of the filter, and the last part contains the hash (to double check if all pieces are arrived).
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct RetainRequest {
    #[prost(message, optional, tag = "1")]
    pub creation_date: ::core::option::Option<::prost_types::Timestamp>,
    #[prost(bytes = "vec", tag = "2")]
    pub filter: ::prost::alloc::vec::Vec<u8>,
    #[prost(enumeration = "super::orders::PieceHashAlgorithm", tag = "3")]
    pub hash_algorithm: i32,
    #[prost(bytes = "vec", tag = "4")]
    pub hash: ::prost::alloc::vec::Vec<u8>,
}
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct RetainResponse {}
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct RestoreTrashRequest {}
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct RestoreTrashResponse {}
/// PieceHeader is used in piece storage to keep track of piece attributes.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PieceHeader {
    /// the storage format version being used for this piece. The piece filename should agree with this.
    /// The inclusion of this field is intended to aid repairability when filenames are damaged.
    #[prost(enumeration = "piece_header::FormatVersion", tag = "1")]
    pub format_version: i32,
    /// content hash of the piece
    #[prost(bytes = "vec", tag = "2")]
    pub hash: ::prost::alloc::vec::Vec<u8>,
    /// the algorithm of the hash
    #[prost(enumeration = "super::orders::PieceHashAlgorithm", tag = "6")]
    pub hash_algorithm: i32,
    /// timestamp when upload occurred, as given by the "timestamp" field in the original orders.PieceHash
    #[prost(message, optional, tag = "3")]
    pub creation_time: ::core::option::Option<::prost_types::Timestamp>,
    /// signature from uplink over the original orders.PieceHash (the corresponding PieceHashSigning
    /// is reconstructable using the piece id from the piecestore, the piece size from the
    /// filesystem (minus the piece header size), and these (hash, upload_time, signature) fields).
    #[prost(bytes = "vec", tag = "4")]
    pub signature: ::prost::alloc::vec::Vec<u8>,
    /// the OrderLimit authorizing storage of this piece, as signed by the satellite and sent by
    /// the uplink
    #[prost(message, optional, tag = "5")]
    pub order_limit: ::core::option::Option<super::orders::OrderLimit>,
}
/// Nested message and enum types in `PieceHeader`.
pub mod piece_header {
    #[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
    #[repr(i32)]
    pub enum FormatVersion {
        FormatV0 = 0,
        FormatV1 = 1,
    }
    impl FormatVersion {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::FormatV0 => "FORMAT_V0",
                Self::FormatV1 => "FORMAT_V1",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "FORMAT_V0" => Some(Self::FormatV0),
                "FORMAT_V1" => Some(Self::FormatV1),
                _ => None,
            }
        }
    }
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ExistsRequest {
    #[prost(bytes = "vec", repeated, tag = "1")]
    pub piece_ids: ::prost::alloc::vec::Vec<::prost::alloc::vec::Vec<u8>>,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ExistsResponse {
    /// input piece ids indices of the missing pieces
    #[prost(uint32, repeated, tag = "1")]
    pub missing: ::prost::alloc::vec::Vec<u32>,
    /// Length contract: either empty (responder predates this field or opted out),
    /// or exactly len(piece_ids) entries aligned by index with the request.
    /// Any other length is a protocol violation; readers MUST verify len(storage_method)
    /// against len(piece_ids) before indexing, and treat mismatched or missing entries
    /// as STORAGE_METHOD_UNSPECIFIED. `missing` remains authoritative for existence;
    /// storage_method\[i\] for indices in `missing` is undefined and MUST be ignored.
    #[prost(enumeration = "StorageMethod", repeated, tag = "2")]
    pub storage_method: ::prost::alloc::vec::Vec<i32>,
}
/// StorageMethod identifies which backend a piece is stored in.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum StorageMethod {
    /// missing, or responder too old to report
    Unspecified = 0,
    /// classic filestore backend
    Piecestore = 1,
    Hashstore = 2,
}
impl StorageMethod {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "STORAGE_METHOD_UNSPECIFIED",
            Self::Piecestore => "STORAGE_METHOD_PIECESTORE",
            Self::Hashstore => "STORAGE_METHOD_HASHSTORE",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "STORAGE_METHOD_UNSPECIFIED" => Some(Self::Unspecified),
            "STORAGE_METHOD_PIECESTORE" => Some(Self::Piecestore),
            "STORAGE_METHOD_HASHSTORE" => Some(Self::Hashstore),
            _ => None,
        }
    }
}
